# Runtime dependencies for main.py
httpx>=0.27.0,<1.0
numpy>=1.26.0,<3.0
psutil>=5.9.0,<8.0
weaviate-client>=4.6.0,<5.0
cryptography>=42.0.0,<47.0

# Optional homomorphic-encryption backend (install separately when CKKS is desired):
# tenseal>=0.3.16

# Research-paper rebuild dependencies
matplotlib>=3.8.0,<4.0
python-docx>=1.1.0,<2.0
